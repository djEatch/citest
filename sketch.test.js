const sum = require('./sketch');

// test('adding 1 to 2 to equal 3', () => {
//     expect(sum(1,2)).toBe(3);
// });

test('adding 1 to 2 to equal 3',);

function sumTest() {
    expect(sum(1,2)).toBe(3);
}